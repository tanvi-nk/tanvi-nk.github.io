<html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>About - Tanvi Kulkarni</title>
    <style>
        /* Base Reset */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        /* Page Layout and Background */
        body {
            background-color: #000000;
            color: #ffffff;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
            padding: 20px;
            overflow-x: hidden;
        }

        /* Electric Blue Edge Accents (Cyberpunk/Tech subtle style) */
        body::before, body::after {
            content: "";
            position: absolute;
            background-color: #00d2ff;
            box-shadow: 0 0 15px #00d2ff, 0 0 30px #00d2ff;
            z-index: 10;
        }

        /* Top Edge Line */
        body::before {
            top: 0;
            left: 5%;
            width: 90%;
            height: 2px;
        }

        /* Bottom Edge Line */
        body::after {
            bottom: 0;
            left: 5%;
            width: 90%;
            height: 2px;
        }

        /* Left/Right Edge Accents (Corner brackets style) */
        .edge-left-accent {
            position: fixed;
            left: 0;
            top: 20%;
            width: 2px;
            height: 60%;
            background: linear-gradient(to bottom, transparent, #00d2ff, transparent);
            box-shadow: 0 0 10px #00d2ff;
        }

        .edge-right-accent {
            position: fixed;
            right: 0;
            top: 20%;
            width: 2px;
            height: 60%;
            background: linear-gradient(to bottom, transparent, #00d2ff, transparent);
            box-shadow: 0 0 10px #00d2ff;
        }

        /* Main Profile Container */
        .container {
            max-width: 650px;
            width: 100%;
            padding: 40px;
            border: 1px solid rgba(0, 210, 255, 0.15);
            background: rgba(10, 10, 12, 0.8);
            border-radius: 12px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.7), inset 0 0 20px rgba(0, 210, 255, 0.05);
            backdrop-filter: blur(10px);
            transition: all 0.3s ease;
        }

        .container:hover {
            border-color: rgba(0, 210, 255, 0.4);
            box-shadow: 0 15px 50px rgba(0, 210, 255, 0.1), inset 0 0 30px rgba(0, 210, 255, 0.08);
        }

        /* Typography */
        h1 {
            font-size: 2.8rem;
            font-weight: 800;
            letter-spacing: -0.5px;
            margin-bottom: 8px;
            background: linear-gradient(135deg, #ffffff 60%, #00d2ff 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .subheading {
            font-size: 1.1rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 2px;
            color: #00d2ff;
            margin-bottom: 24px;
            text-shadow: 0 0 8px rgba(0, 210, 255, 0.4);
        }

        .bio {
            font-size: 1.15rem;
            line-height: 1.7;
            color: #b0b3b8;
            margin-bottom: 40px;
        }

        /* Section: Current Mission */
        .mission-section {
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            padding-top: 30px;
        }

        h2.section-title {
            font-size: 1.2rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            color: #ffffff;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
        }

        h2.section-title::after {
            content: "";
            flex-grow: 1;
            margin-left: 15px;
            height: 1px;
            background: linear-gradient(to right, rgba(0, 210, 255, 0.5), transparent);
        }

        /* Mission List */
        .mission-list {
            list-style: none;
        }

        .mission-item {
            font-size: 1.1rem;
            color: #e3e4e6;
            margin-bottom: 14px;
            display: flex;
            align-items: center;
            padding: 8px 12px;
            background: rgba(255, 255, 255, 0.02);
            border-left: 3px solid #00d2ff;
            border-radius: 0 6px 6px 0;
            transition: transform 0.2s ease, background 0.2s ease;
        }

        .mission-item:hover {
            transform: translateX(5px);
            background: rgba(0, 210, 255, 0.05);
        }

        /* Responsive Optimization */
        @media (max-width: 480px) {
            h1 { font-size: 2.2rem; }
            .subheading { font-size: 0.9rem; }
            .container { padding: 25px; }
            .bio { font-size: 1rem; }
        }
    </style>
</head>
<body>

    <!-- Edge Lines Dynamic Accents -->
    <div class="edge-left-accent"></div>
    <div class="edge-right-accent"></div>

    <div class="container">
        <header>
            <h1>Tanvi Kulkarni</h1>
            <!-- Rephrased "EXPLORER OF ALL THINGS" to sound more professional and poetic -->
            <div class="subheading">An Explorer of Endless Possibilities</div>
        </header>

        <main>
            <p class="bio">
                I explore filmmaking, science, and story writing—experimenting with ideas and bringing creative projects to life inspired by the magic of movies.
            </p>

            <section class="mission-section">
                <h2 class="section-title">Current Mission</h2>
                <ul class="mission-list">
                    <li class="mission-item">Mastering Filmmaking &amp; Photography</li>
                    <li class="mission-item">Conducting In-depth Research</li>
                    <li class="mission-item">Authoring a Book</li>
                    <li class="mission-item">Mastering the Ukulele</li>
                </ul>
            </section>
        </main>
    </div>

</body>
</html>
